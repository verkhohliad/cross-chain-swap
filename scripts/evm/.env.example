# Passet Hub Testnet (.env)
# Rename this file to ".env" and fill in missing values.
# Make sure the PrivateKey has testnet funds for gas on Passet Hub.

# EVM RPC + Chain
RPC=https://testnet-passet-hub-eth-rpc.polkadot.io
CHAIN_ID=420420422

# Account (hex private key without 0x or with 0x; ethers v6 accepts both)
PrivateKey=0xYOUR_PRIVATE_KEY_HERE

# 1inch Limit Order Protocol V4 (verifying contract / LOP)
# These two typically point to the same address (the LOP v4).
VERIFYING_CONTRACT=0xLOP_V4_ADDRESS_ON_PASSET_HUB
LIMIT_ORDER_PROTOCOL=0xLOP_V4_ADDRESS_ON_PASSET_HUB

# 1inch Cross-Chain Swap Escrow Factory (on Passet Hub)
ESCROW_FACTORY=0xESCROW_FACTORY_ADDRESS_ON_PASSET_HUB

# Deployed Resolver (after running deployResolver.js)
RESOLVER_ADDRESS=0xYOUR_DEPLOYED_RESOLVER_ADDRESS

# “Always-true” ERC20 used in Fusion+ cross-chain orders (per 1inch design for takerAsset)
ERC20_TRUE=0xERC20_TRUE_ON_PASSET_HUB

# Stablecoin for example (USDC or other ERC-20 on Passet Hub testnet)
USDC=0xUSDC_ON_PASSET_HUB
